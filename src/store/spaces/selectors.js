export const selectSpaces = (reduxState) => reduxState.spaces.spaces;
export const selectSpaceWithStories = (reduxState) =>
  reduxState.spaces.spaceWithStories;
export const selectMe = (reduxState) => reduxState.spaces.me;
